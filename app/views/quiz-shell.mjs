// Marcação comum às sessões curadas (#/quiz/:specId) e dinâmicas (#/sessao).
// Um único motor (`window.QuestoesApp.initQuiz`) e uma única marcação — a
// diferença entre os dois tipos de sessão fica só no cabeçalho (`extraHtml`).
export const escapeText = value => String(value ?? "").replace(/[&<>"']/g, ch => ({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[ch]));

export function quizShellHtml({ eyebrow, backHref, backLabel, extraHtml = "" }) {
  return `<section class="quiz-view"><div class="quiz-toolbar-top"><a href="${backHref}">← ${backLabel}</a><button type="button" class="settings-trigger" aria-haspopup="dialog">⚙ Configurações</button></div><header class="quiz-heading"><span class="eyebrow">${escapeText(eyebrow)}</span><h1 id="quiz-title"></h1><p id="quiz-desc" class="muted"></p></header>${extraHtml}<div class="container"><div class="editorial-notice" role="note">Banco educacional em auditoria editorial progressiva. Consulte a <a href="_banco/POLITICA-EDITORIAL.md">política editorial</a>.</div><div class="toolbar"><div class="score">Acertos: <span id="score-correct">0</span> · Parciais: <span id="score-partial">0</span> · <span id="score-total">0</span>/<span id="score-max">0</span> respondidas</div><div class="toolbar-right"><div class="save-actions"><button class="export export-progress" id="save-progress">💾 Salvar progresso</button><button class="export export-quality" id="save-quality">✎ Sugestões (<span id="quality-pending">0</span>)</button></div><button class="reset" id="reset-quiz">Reiniciar</button></div></div><div class="save-guidance"><p><strong>Progresso:</strong> escolha <code>_dados/registros/</code>.</p><p><strong>Sugestões:</strong> escolha <code>_dados/melhorias de questões/</code>.</p></div><p class="export-hint" id="export-hint">Arquivo baixado. Mova-o para <code>Questões/_dados/registros/</code>.</p><nav class="focus-navigation" aria-label="Navegação entre questões"><button type="button" id="focus-prev">← Anterior</button><span id="focus-position" class="focus-position" aria-live="polite"></span><button type="button" id="focus-next">Próxima →</button></nav><div id="quiz"></div><div class="refs"><h2>Referências</h2><ol id="ref-list"></ol></div><p class="footnote" id="quiz-footnote"></p></div></section>`;
}
