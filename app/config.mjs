// Configuração de publicação. Sem bundler/variáveis de ambiente neste
// projeto (é HTML/ESM estático), então isso é literalmente a config: edite
// o valor abaixo quando a URL pública mudar (novo domínio, novo host etc).
//
// Quando preenchida, é a base oficial usada por getPublicAppBaseUrl() para
// montar links de compartilhamento — nunca localhost, mesmo que o link seja
// copiado a partir do servidor de dev (`node tools/serve.js`).
// Deixe "" apenas se o site ainda não tiver URL pública (aí o link cai de
// volta na origem atual, o que só funciona para quem tem o mesmo servidor
// local rodando).
export const PUBLIC_APP_URL = "https://viniciuskato.github.io/questoes";
